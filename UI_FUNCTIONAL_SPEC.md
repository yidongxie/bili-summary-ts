# BiliStudy 当前网页功能与交互说明

本文档用于给其他 agent 重新设计 UI 时参考。重点描述当前网页已有功能、页面结构、核心用户流程、状态变化和关键交互逻辑，不限定视觉风格。

## 1. 产品定位

BiliStudy 是一个面向 B 站视频学习的网页工具。用户粘贴 B 站视频链接后，系统获取视频信息、转写视频语音、生成 AI 总结和标签，用户可以保存到个人收藏库，并进一步导出为 PDF、Markdown 或发送到 Obsidian。

核心目标：

- 把 B 站视频转换成结构化学习笔记。
- 支持用户收藏、搜索、筛选和复习视频笔记。
- 支持导出到本地文件或 Obsidian 知识库。
- 支持多用户登录，每个用户拥有独立配置和收藏库。

## 2. 当前整体信息架构

当前网页是一个单页应用，主要由左侧导航和右侧内容区组成。

### 主导航

共有 3 个主页面：

1. **总结**
   - 输入视频链接。
   - 选择总结模式和分类。
   - 发起总结任务。
   - 查看视频、转写文本、AI 总结、标签和笔记。
   - 保存到收藏库。

2. **收藏库**
   - 查看已保存的视频总结卡片。
   - 按关键词、分类、标签筛选。
   - 打开详情。
   - 导出 PDF / Markdown。
   - 发送到 Obsidian。
   - 删除收藏。

3. **设置**
   - 配置 DeepSeek / OpenAI 兼容模型。
   - 配置 Whisper 转写 API。
   - 配置 Obsidian 导出方式。

### 登录入口

左侧底部有登录区域：

- 未登录：显示“登录”按钮。
- 已登录：显示用户显示名或邮箱，并显示“退出登录”。

登录和注册使用居中的弹窗表单。

## 3. 用户账号与登录流程

### 3.1 未登录状态

未登录用户可以看到页面，但关键操作会被拦截：

- 开始总结时提示“请先登录”。
- 保存设置时提示“请先登录”。
- 收藏库返回空数据。

### 3.2 登录弹窗

登录弹窗包含两个模式：

#### 登录模式

字段：

- 邮箱
- 密码

操作：

- 点击“登录”后按钮变为“登录中...”，并禁用。
- 登录成功后刷新页面。
- 登录失败时在表单内显示错误信息。

校验：

- 邮箱和密码不能为空。

#### 注册模式

字段：

- 邮箱
- 显示名称，可选，默认邮箱前缀。
- 密码，至少 6 位。

操作：

- 点击“注册”后按钮变为“注册中...”，并禁用。
- 注册成功后刷新页面。
- 注册失败时在表单内显示错误信息。

校验：

- 邮箱和密码不能为空。
- 密码至少 6 位。

### 3.3 弹窗切换逻辑

- 登录表单底部有“还没有账号？注册”。
- 注册表单底部有“已有账号？登录”。
- 点击“取消”关闭弹窗。
- 打开弹窗时默认展示登录表单，并聚焦邮箱输入框。

## 4. “总结”页面功能

这是产品的主流程页面。

### 4.1 初始输入区

页面顶部说明：粘贴 B 站链接，生成可以收藏和复习的学习笔记。

输入区包含：

1. **B站视频链接输入框**
   - 占位示例：`https://www.bilibili.com/video/BV...`
   - 页面加载后自动聚焦。

2. **清空按钮**
   - 清空链接输入框。
   - 清空后重新聚焦输入框。

3. **开始总结按钮**
   - 发起异步总结任务。

4. **总结模式选择**
   - 简洁总结：`brief`
   - 详细笔记：`detailed`
   - 时间线：`timeline`
   - 知识卡片：`knowledge`

5. **保存分类输入框**
   - 默认值来自用户配置。
   - 当前默认通常是“待整理”。
   - 保存收藏时会作为该条笔记的分类。

6. **状态提示区**
   - 用于显示错误、处理中、成功等信息。

### 4.2 开始总结交互

用户点击“开始总结”后：

1. 前端校验：
   - 如果链接为空，显示“请输入视频链接”。
   - 如果未登录，显示“请先登录”。

2. 校验通过后：
   - “开始总结”按钮禁用。
   - 状态提示显示“正在提交任务...”。
   - 结果区域展开。
   - 显示居中的加载状态和 spinner。
   - 加载文案初始为“正在提交任务...”。

3. 前端提交总结任务：
   - 请求 `POST /api/tasks/summarize`。
   - 请求内容包含：视频 URL、总结模式、模型配置、Whisper 配置。

4. 服务端返回 `task_id` 后：
   - 前端通过 SSE 连接 `/api/tasks/:task_id/events` 监听任务进度。

### 4.3 总结任务进度状态

服务端通过 SSE 推送状态。当前主要进度文案包括：

- 排队中…
- 获取视频信息…
- 获取视频元数据…
- 语音转写中…
- 字幕过长时：提示已自动截取前后内容用于总结。
- AI 总结中…
- 生成标签…

UI 设计时应保留一个明显的任务进度区域，用于展示当前阶段。

### 4.4 总结失败状态

失败时：

- 关闭加载 overlay。
- 恢复“开始总结”按钮可点击。
- 在状态提示区显示错误。

常见错误：

- 未登录。
- 未填写 DeepSeek / OpenAI 兼容 API Key。
- 未填写 Whisper API Key。
- 无法识别视频链接。
- 无法获取视频信息。
- 无法获取视频音频信息。
- 语音转写失败。
- 总结任务连接中断。
- 今日总结次数达到上限。

如果错误包含未登录信息，当前逻辑会自动打开登录弹窗。

### 4.5 总结成功后的结果页

总结完成后，结果区域替换加载状态，显示完整结果界面。

结果页主要分为：

1. 顶部操作条
2. 左侧视频与视频文本
3. 右侧 AI 总结、标签和个人笔记

#### 4.5.1 顶部操作条

包含：

- **返回按钮**
  - 收起结果区域。
  - 焦点回到视频链接输入框。
  - 不清空当前结果数据。

- **结果标题**
  - 显示视频标题。

- **复制总结按钮**
  - 把 AI 总结复制到剪贴板。
  - 成功后显示“总结已复制”。

- **保存 / 已收藏按钮**
  - 未保存时显示“保存”。
  - 保存成功或检测到已保存时显示“已收藏 ✓”，按钮禁用。

#### 4.5.2 视频信息区

显示：

- 标题
- UP 主
- 时长
- 原视频链接
- B 站播放器 iframe

播放器嵌入地址基于 bvid：

- `https://player.bilibili.com/player.html?bvid=<bvid>&autoplay=0&high_quality=1`

UI 设计时需要给视频播放器保留 16:9 容器。

#### 4.5.3 视频文本区

显示语音转写结果。

当前展示方式：

- 标题为“视频文本”。
- 右侧有“复制文本”按钮。
- 文本按时间聚合成段落。
- 每段左侧显示开始时间，格式如 `03:25`。
- 右侧显示该时间段对应文本。

段落聚合逻辑：

- 过滤空文本片段。
- 如果当前片段与前一片段之间间隔大于等于 3 秒，或当前片段距离前一片段开始时间超过约 9 秒，则开启新段落。
- 同一段落内文本用中文句号连接。

空状态：

- 没有文本时显示“未获取到字幕文本。”或“无可用字幕文本。”

虽然界面文案叫“视频文本”，当前后端实际强制使用 Whisper 语音转写，不再优先使用 B 站字幕。

#### 4.5.4 AI 总结区

显示 AI 生成的 Markdown 总结。

当前前端支持的 Markdown 展示较简单：

- `#` / `##` / `###` 标题
- `**加粗**`
- 行内代码
- `-` 列表
- 换行和段落

UI 重设计时可以用更完整的 Markdown 渲染，但需要保持总结正文层级清晰、可阅读。

#### 4.5.5 标签区

总结完成后系统会生成建议标签。

交互：

- 建议标签以 chip 形式展示。
- 默认全部选中。
- 点击某个标签 chip 可以切换选中 / 取消选中。
- 选中的标签有明确视觉状态，并带 `✓` 标识。
- 用户可以在“自定义标签”输入框继续输入额外标签。
- 保存时会合并“选中的建议标签”和“自定义标签”。

自定义标签解析规则：

- 支持逗号、中文逗号、分号、中文分号、空格分隔。
- 自动去掉标签开头的 `#`。
- 自动去重。

重新生成标签：

- 有“重新生成”按钮。
- 总结完成前禁用。
- 点击后按钮文字变为“生成中...”，并禁用。
- 请求 `POST /api/suggest-tags`。
- 生成成功后替换当前建议标签，并默认全部选中。
- 失败时显示“生成标签失败：...”

后端已提供 `POST /api/suggest-tags`，会使用当前用户保存的 DeepSeek / OpenAI 兼容配置生成新标签。

#### 4.5.6 我的笔记

用户可以在结果页输入自己的笔记。

保存收藏时，个人笔记会和视频信息、总结、转写文本、分类、标签一起保存。

### 4.6 保存到收藏库

点击“保存”后提交：

- 视频信息
- AI 总结
- 完整转写文本
- 文本片段数量
- 总结模式
- 当前分类
- 标签
- 我的笔记

保存成功后：

- 状态提示显示“已保存：视频标题”。
- 刷新收藏库数据。
- 保存按钮变为“已收藏 ✓”并禁用。

保存失败时：

- 状态提示显示“保存失败：错误信息”。

重复保存检测：

- 总结完成后会根据 bvid 调用 `/api/library/check/:bvid`。
- 如果已经保存过，保存按钮直接变成“已收藏 ✓”。

## 5. “收藏库”页面功能

收藏库用于管理已保存的视频学习资料。

### 5.1 搜索与筛选区

顶部工具栏包含：

1. **搜索输入框**
   - 占位文案：搜索标题、UP主、总结、标签。
   - 按 Enter 会触发搜索。

2. **分类筛选下拉框**
   - 默认“全部分类”。
   - 分类来自当前查询结果中的分类集合。

3. **标签筛选下拉框**
   - 默认“全部标签”。
   - 标签来自当前查询结果中的标签集合。
   - 展示时带 `#` 前缀。

4. **搜索按钮**
   - 点击后重新请求收藏库列表。

筛选请求：

- `GET /api/library?q=<keyword>&category=<category>&tag=<tag>`

筛选逻辑：

- 关键词匹配标题、UP 主、总结、标签。
- 分类要求完全匹配。
- 标签要求完全匹配，大小写不敏感。

### 5.2 收藏卡片列表

收藏以卡片网格展示。

每张卡片包含：

- 视频标题
- UP 主
- 分类
- 保存时间
- 标签列表
- 操作按钮

操作按钮：

1. **打开**
   - 打开详情面板。

2. **导出 PDF**
   - 下载可打印 HTML，浏览器自动触发打印，可另存为 PDF。

3. **送进 Obsidian**
   - 按设置中的 Obsidian 导出方式执行。

4. **导出 Markdown**
   - 下载 `.md` 文件。

5. **删除**
   - 弹出确认框。
   - 确认后删除收藏。

空状态：

- 没有收藏时显示“还没有收藏。先总结一个视频，然后保存到收藏库。”

### 5.3 打开收藏详情

点击“打开”后：

- 请求 `GET /api/library/:id`。
- 页面下方显示详情面板。
- 自动滚动到详情区域。

详情内容包括：

- 标题
- 操作按钮：打开原视频、导出 PDF、送进 Obsidian、导出 Markdown
- UP 主、分类、保存时间
- 标签
- B 站播放器
- AI 总结
- 我的笔记，如果存在

详情页目前不展示完整转写文本，只展示总结和用户笔记。

### 5.4 删除收藏

点击删除时：

1. 浏览器确认框：`确定删除这条收藏吗？`
2. 用户取消：不做任何事。
3. 用户确认：请求 `DELETE /api/library/:id`。
4. 删除后：
   - 隐藏详情面板。
   - 重新加载收藏库。

删除后会重新检查当前总结结果的收藏状态，避免把已删除内容误标记为已收藏。

## 6. 导出功能

### 6.1 导出 PDF

入口：

- 收藏卡片按钮。
- 收藏详情按钮。

行为：

- 请求 `/api/export/:id.pdf`。
- 后端返回可打印 HTML。
- HTML 页面加载后自动调用 `window.print()`。
- 用户通过浏览器打印功能另存为 PDF。

导出内容：

- 标题
- UP 主
- 时长
- 分类
- 保存时间
- 标签
- 原视频链接
- AI 总结
- 我的笔记，如果存在

### 6.2 导出 Markdown

入口：

- 收藏卡片按钮。
- 收藏详情按钮。

行为：

- 请求 `/api/export/:id.md`。
- 浏览器下载 Markdown 文件。

Markdown 内容：

- YAML frontmatter：标题、来源、作者、站点、bvid、时长、分类、总结模式、文本条数、创建更新时间、标签。
- H1 标题。
- Source 信息块。
- B 站播放器嵌入链接。
- AI 总结。
- 我的笔记，如果存在。

### 6.3 送进 Obsidian

入口：

- 收藏卡片按钮。
- 收藏详情按钮。

Obsidian 导出只使用浏览器 URI 模式：服务器生成 Markdown，浏览器通过 `obsidian://new` 拉起用户本机 Obsidian 客户端。

适用场景：远程部署，服务器无法直接写入用户本机 vault。

导出流程：

1. 请求 `GET /api/export/:id/obsidian-payload` 获取 Markdown 内容和目标文件路径。
2. 前端构造 `obsidian://new` URI。
3. 通过浏览器唤起本机 Obsidian 客户端。

URI 参数：

- `vault`：可选，来自设置中的 Vault 名称。
- `name`：目标笔记路径，不带 `.md`。
- `content`：Markdown 内容。

长内容兜底：

- 如果 URI 长度超过约 7500 字符，前端会尝试把 Markdown 写入剪贴板。
- 然后构造只包含 `vault` 和 `name` 的较短 `obsidian://new` URI。
- 页面提示用户在 Obsidian 中粘贴，或手动新建笔记后 Ctrl+V。

## 7. “设置”页面功能

设置页用于配置生成总结、语音转写和导出所需的参数。

### 7.1 DeepSeek API 设置

用途：生成视频总结和标签。

字段：

- API Key，密码输入框。
- 模型，默认 `deepseek-chat`。
- API 地址，默认 `https://api.deepseek.com/v1`。

状态：

- 如果后端已有加密保存的 key，前端不回填明文，只显示“✓ 已配置”。
- 如果没有配置，显示“未配置”。

保存逻辑：

- API Key 输入框为空时，不覆盖原有 key。
- 只有用户输入新 key 时才更新。

### 7.2 Whisper API 设置

用途：把视频音频转写为文本。

字段：

- API Key，密码输入框。
- API 地址，默认 `https://api.siliconflow.cn/v1`。
- 模型，默认 `FunAudioLLM/SenseVoiceSmall`。

状态逻辑与 DeepSeek API Key 相同。

### 7.3 Obsidian 设置

字段：

1. **Vault 名称**
   - 是 Obsidian 侧栏顶部显示的 vault 名称。
   - 可留空，留空时由 Obsidian 默认 vault 接收。

2. **Vault 中的子文件夹**
   - 默认 `BiliStudy`。
   - 用于指定导出笔记放在 vault 内哪个文件夹。

### 7.4 保存设置

点击“保存设置”后：

- 未登录：显示“请先登录”。
- 已登录：提交 `POST /api/config`。
- 保存成功：显示“设置已保存”。
- 保存失败：显示错误信息。

注意：当前前端 payload 没有提交 `default_category`，但总结页会读取 `default_category` 作为分类默认值。若重设计中希望用户能设置默认分类，需要补充对应 UI 和提交字段。

## 8. 后端异步任务与限制

### 8.1 总结任务异步机制

总结不是同步请求完成，而是两步：

1. `POST /api/tasks/summarize` 创建任务。
2. `GET /api/tasks/:id/events` 使用 SSE 接收进度和结果。

UI 设计时应避免做成普通 loading 后等待 HTTP 返回，而应保留实时进度展示。

### 8.2 每日次数限制

普通用户每天有总结次数上限。

- 默认上限：10 次 / 天。
- 可通过环境变量 `MAX_DAILY_SUMMARIES` 调整。
- 管理员邮箱 `444925817@qq.com` 不受此限制。

达到上限时返回 429，错误文案类似：

- 今日总结次数已达上限（10 次）

### 8.3 长文本截断

后端会限制进入总结模型的文本长度。

- 默认最大转写文本字符数：60000，可由 `MAX_SUBTITLE_CHARS` 配置。
- 过长时保留前 70% 和后 30%，中间插入省略说明。
- 总结末尾追加提示，说明总结基于截取内容生成。

## 9. 数据对象概念

### 9.1 视频结果对象

总结完成后的结果大致包含：

- `video.title`：标题
- `video.author`：UP 主
- `video.duration`：时长，秒
- `video.bvid`：B 站 bvid
- `video.link`：原视频链接
- `video.pic`：封面图
- `subtitle_count`：文本片段数量
- `transcript_source`：当前为 `whisper`
- `subtitle_segments`：带时间的文本片段
- `transcript`：完整文本
- `summary`：AI 总结 Markdown
- `mode`：总结模式
- `suggested_tags`：建议标签

### 9.2 收藏项对象

收藏库条目包含：

- `id`
- `created_at`
- `updated_at`
- `title`
- `author`
- `duration`
- `bvid`
- `link`
- `summary`
- `transcript`
- `subtitle_count`
- `category`
- `tags`
- `notes`
- `mode`

## 10. 关键 UI 状态清单

重设计时建议覆盖以下状态：

### 全局状态

- 未登录
- 已登录
- 登录弹窗打开
- 注册弹窗打开
- 登录 / 注册提交中
- 登录 / 注册错误

### 总结页状态

- 初始输入状态
- 链接为空错误
- 未登录错误
- 提交任务中
- SSE 连接中
- 任务多阶段进度
- 任务失败
- 任务成功，有完整结果
- 任务成功但文本为空或无可用文本
- 结果已保存
- 结果未保存
- 复制总结成功
- 复制文本成功
- 标签重新生成中
- 标签重新生成失败

### 收藏库状态

- 加载列表
- 空列表
- 有卡片列表
- 搜索 / 分类 / 标签筛选后无结果
- 详情打开
- 删除确认
- 删除后刷新
- 下载失败 alert

### 设置页状态

- key 未配置
- key 已配置
- 保存中，可选
- 保存成功
- 保存失败

### Obsidian 导出状态

- URI 唤起中
- URI 过长，复制到剪贴板兜底
- URI 唤起失败

## 11. 当前页面中不明显但存在的能力

当前总结任务逻辑强制使用 Whisper 语音转写，不走 B 站官方字幕 fallback。UI 文案应优先使用“视频文本”“语音转写”，避免让用户误以为一定使用官方字幕。

## 12. 适合新 UI 保留的核心交互原则

1. **总结流程要有强进度感**
   - 视频处理可能耗时较长，必须显示当前阶段，而不是只有一个静态 loading。

2. **结果页应支持边看视频边看总结**
   - 当前布局左侧视频 / 文本，右侧总结 / 标签 / 笔记，适合学习场景。
   - 新 UI 可以调整布局，但需要保留视频、文本、总结的并列访问能力。

3. **保存前允许用户整理标签和笔记**
   - 标签和个人笔记是保存前的重要编辑区。

4. **收藏库应突出“复习”和“导出”**
   - 卡片要快速展示标题、UP 主、分类、标签和保存时间。
   - 导出动作是核心功能，不应藏太深。

5. **设置页要强调敏感凭证状态而不是明文**
   - API Key、Whisper Key 不回显。
   - 用“已配置 / 未配置”状态提示用户。

6. **Obsidian 导出需要解释清楚**
   - 远程部署只通过浏览器拉起本机 Obsidian。
   - URI 模式适合远程部署。

## 13. 主要接口参考

### 认证

- `GET /api/auth/me`：获取当前登录状态。
- `POST /api/auth/login`：登录。
- `POST /api/auth/register`：注册。
- `POST /api/auth/logout`：退出登录。

### 配置

- `GET /api/config`：获取公开配置和 key 状态。
- `POST /api/config`：保存配置。

### 总结任务

- `POST /api/tasks/summarize`：创建总结任务。
- `GET /api/tasks/:id/events`：SSE 监听任务状态。

### 收藏库

- `GET /api/library`：获取收藏列表，支持 q、category、tag。
- `GET /api/library/check/:bvid`：检查视频是否已收藏。
- `GET /api/library/:id`：获取收藏详情。
- `POST /api/library`：保存收藏。
- `DELETE /api/library/:id`：删除收藏。

### 导出

- `GET /api/export/:id.pdf`：导出可打印 HTML / PDF。
- `GET /api/export/:id.md`：下载 Markdown。
- `GET /api/export/:id/obsidian-payload`：获取 Obsidian URI 导出所需 Markdown。

### 标签

- `POST /api/suggest-tags`：重新生成标签。
